import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        padding: 20,
        borderRadius: 16,
        justifyContent: 'center',
        alignItems: 'center',
        gap: 10,
        textAlign: 'center',
        borderWidth: 1,
    }
})
